import sys
import os

filename = "Python Programs/Programming Fundamentals/External Data/Data/characters.txt"

characterList = []
def searchCharacters(_filename):
    with open(_filename, "r") as file:
        characterList = file.read()
    file.close()
    for character in characterList:
        match character:
            case character if character = "a":
                aCount += 1
            case character if character = 
            
            
